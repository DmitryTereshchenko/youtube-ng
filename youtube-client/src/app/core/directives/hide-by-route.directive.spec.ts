import { HideByRouteDirective } from './hide-by-route.directive';

describe('HideByRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new HideByRouteDirective();
    expect(directive).toBeTruthy();
  });
});
