import { HighlightShadowDirective } from './highlight-shadow.directive';

describe('HighlightShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightShadowDirective();
    expect(directive).toBeTruthy();
  });
});
